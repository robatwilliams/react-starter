// Libraries loaded from this entry point are specifically excluded from the vendor bundle.
// Each polyfill needs a corresponding check in the polyfill conditional loader.
import 'core-js/modules/es6.promise';
import 'whatwg-fetch';
